ere is anything that we canIf we want to keep it only inside the class, then we can make it private. Now this concept of private and public works like it works in other languages, it has become Java, it has become C , if you have a background in those languages, then it does not work exactly like that. oops pythonanywhere.com, we should put underscore underscore in front of the functions and also we should put underscore underscore in front of the methods. For example lets.Create Another Class We create a class from a normal one. Let s suppose this is a class called Person. Inside the Person class, we create a text inside which we are storing the name. Anonymous. In this way, we have created a class. Now p1 is equal to this new person which has been created for us. If we try to print the name of p1 here, then now we will get no error.