ngs Null String Now, how to pass this string to the function, pass it like a normal character array. Here we write the character array and here we do not need to send the size of the line because a special thing about the string is that the string ends where the null character comes. Inside the normal array, we do not know how many elements are there inside the array and till how long it will continue, but inside the string we know that as soon as the null character comes i.e. ourIf the string is over then we will not pass the size here. Let s write this function void print string inside which we will have our string. Now inside our function we will create a loop. We will start the loop with an integer aa which is starting from zero and every time aa will be our plus plus. Now let s postpone this condition for a while and what will we do inside. Every time we are going to print our characters by doing print F.