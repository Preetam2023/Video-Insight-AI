dress operator, value at address operator and this M percentage is what we call address of operator. Whoever we put this end in front of, we get its address. Whoever we put a star in front of, we get its value. What does it mean? Here we made a pointer a variable, created a pointer variable, how did we know that the pointer is a variable because the risk seems to be that of a pointer variable.In the syntax and what did we do to it, the address of the edge i.e. whatever address of the edge was now got stored inside this pointer i.e. this location came inside the memory where the pointer variable came and inside it got stored the address of the edge that is 2010 and now if we want to see what value is stored at the address where the pointer is stored, then for that we can use this operator i.e.