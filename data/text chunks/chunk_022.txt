riable, like if we want to store a final price, then we will write int final underscore price e e 1. Now this underscore is not the only special character that can come in the name of our variable. Suppose, instead of this underscore, we give a space here, then hereWe would have got an error because we cannot give a space in the name of the variable or if we had given a comma instead of a space, we would have also got an error because we cannot give a comma. We can use only one special character in a variable, which is the underscore, which brings us to the fourth rule. Which is no other symbol other than the underscore can be used. Now, one special thing about the variable is the value of the variable, isn t it?It is not fixed, it can change like we have defined our age variable because as of today our age is 22 but tomorrow i.e. after 2 years our age will be 24. When it gets updated then how will we write it as age equals 24. So earlier our age variable was 22 i.e.