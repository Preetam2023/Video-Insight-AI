very important concept of this language is our input. To take the input inside C, we use a function.Which is another library function whose name is scan f. Just like we used it to print print f, similarly we will use it to scan anything to take input. Scan f. Now its format is that whatever value we have to take input, first of all we will write its type, then we will specify the format. Percentage D shows us that we are going to take input of an int type value, then first of all we will put our quotes. Double quotes inside it, specify our format, comma, after comma we have toWe have to write that we have understood the type of the value that we are taking as input, now in which variable we have to store it, then we will write the name of our variable and before the name of that variable, we will put M percent. This M percent i.e. end is actually an address. This is the address of this variable, i.e.