hether you have written the code correctly and the compiler also does the work of running that code. So we will be reading about compilers in detail later. Now, first of all, install your code editor i.e. VS Code.So, to install VS Code, we first load VS Code for Wind on our screen. Now here first of all we will get the option that we want to download it for Vis. Next, now a list of check boxes will be coming to us and what we have to do is to tick all the check boxes. The above one says that if an icon is created for us on the desktop and the below one is giving us a lot of information about paths etc. then all the check boxes are ticked.And after that we will install it, then finally VS Code will be installed for us and we will click on Finish. Now after this we can go to the system and search for VS Code. After searching for VS Code, it will open and something like this will be displayed on the screen, inside which first of all we can choose a theme.