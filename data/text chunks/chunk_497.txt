n what will happen inside the memory? All the blocks that we have taken as characters will be saved. We will name these blocks as name, whatever is the name of our variable. Now save a single character inside each block.This will happen because the size of each block is one byte, within which we can save a single character. Each block will have its own address and all these addresses will be contiguous, because if the string is a character array inside the memory, then the memory of the character array is continuous, that is, it is continuous, so the memory of the string will also be continuous and inside each memory block, we store an individual character but one.The special thing in this is that a null character is also stored at the end, i.e.