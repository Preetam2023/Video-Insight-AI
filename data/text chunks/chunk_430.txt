once again, so these three prices have come with.Added GST and total price can be done in this way. So here the biggest benefit that we saw by using Array was that we did not have to maintain three different variables. We created a single variable and from that all our work is running. When we are talking about similar data types, now let s move towards our next topic which is Initialization of Array. Now we saw that we can declare the array.After that, elements can be inserted in it, but the array can also be initialized with declaration, like if we create an int, then we can write it as int x and then x 10 or we can write it as intName your array then square brackets. Now pay attention that we do not need to write the size inside the square brackets because whatever elements we write on the right side will automatically give us the size of the array. After that on the right side we write curly braces and all the elements inside it i.e.