will use it for that. How to do F print F? First of all we write F print F and pass it inside F PTR. Now we first want to write our string then we will write name and after the string we want to put comma end.After the comma, we want the information that is in the next line to go or in fact, we format this information with tab spaces, then we will write F print F F PTR, after this we want to display our edge and pass our edge F print F F PTR Percentage F. Save our CGPA once. First of all, the name will be printed, then the tab space will come, then the edge will be printed, then the tab space will come, then CG will be on, all this will be inside the student file.This is the one which we will create a new one and at the end we will close the file. Now run the code once. The array which is in itself is a pointer, so this character array which is name is itself a pointer, so there is no need to put em percent here, let s clear it and now run.