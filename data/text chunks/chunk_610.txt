me thing.Let s put this code from here and now let s create a new structure named Structure Address. First of all we will store int house number inside it, after that int block, then we will write character city of 100 then character state of 100 and here we have created a structure named pe statement terminator toy address. Now let s make an array of it. Let us shorten the name of the structure address array, then give it the name AIDS and keep its size five one.Let s create a function to print the address information and take input from the user inside the main function. So first of all, to take the input, enter print enter info for person one. For person one, information will be entered. We will scan that information. First of all, we will get the house number, then the ads of 0 dot house number will come to us in the same way. So, we will write here das d0 d block. Now we will get our string i.e. our city, then d.