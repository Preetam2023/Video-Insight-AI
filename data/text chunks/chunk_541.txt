this loop a little more deeply. First of all, we created a string, after that we created characters.Our goal is to take characters from the user one by one and add them inside the string and how will we track the index of the string. Now with the help of our variable, we will create a loop. We are creating a while loop. The condition inside it is that till our user does not press enter, put all the characters given by us in the string. So, we will keep taking input from the scanner one by one. After that, what will we do, we will add them inside the string and the index of the string.We will increase it by one, let s save it and now what will we do with it finally, we will have to put the null character inside the ATR of aa manually because the null character will not be inserted automatically compiled now because the null character is not automatically inserted in the percentage c, if the percentage is put in s then we will have to insert the null character separately to show that it