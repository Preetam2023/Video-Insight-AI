physics marks in it, then int marks two, we will store chemistry marks in it and then int marks th.Now to access these three, we need different variables, but if I say that I can access all three values from a single variable, then we will call that type of structure as array. How to write array, we will write the type of which we are going to create an array, then we will write Marxus, then we will put square brackets which show that the structure that we are creating is an array and after that ourWe will write the marks 97 98 89 in this way, this is the syntax of writing the array in which we have stored three values inside a single variable, then these values will be stored one after the other in the memory and all the data will go inside those three blocks.