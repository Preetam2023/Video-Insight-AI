able n in the main function, then the only function that will recognize this n will be the main one. Now if I create another number n inside the sum, then it will be a separate variable n, so in this way two variables will be created inside the memory, one with the name n will be of our main function.If one is of our second function, then we can also give the same name. We can also give different names by convention. We will give the same name. There is a print table inside which there is a single parameter whose name is int n. Now let s define our print table function. The return type is void. After this we will write the name of our function print table and int n. How to print a table of any number. By putting a loop, first of all make a loop inside this function. The value of i will be starting with one.