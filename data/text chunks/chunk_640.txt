from a new file.The first parameter inside txt.gz f open is the name of the file i.e. test d txt.gz. You will also practice or you can see your old questions of any college. There are many easy questions from this topic, that is, from a topic like array, you can ask very difficult questions, but files are such a topic which is a little complex in itself. If we ask questions from here, they will be easy to us, so let us recap the file named FPTR.Created a pointer, opened the file inside FPTR and assigned value to it. We used the F open function in which we passed two parameters, one is the name of our file and one is our mode. Now we can close the file in the same way as we opened it. The function to close is ours. F close. The syntax of writing F close is very easy. F and then close and inside just put the pointer of the file which we have to close. Now the logic behind closing.