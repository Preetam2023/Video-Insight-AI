into our picture. Now gate s which is a little depreciated, so instead of this we will use f gates.If we read these three in detail, we have three functions for the string. Whenever we want to input and output the string, percentage s is our format specification. Scanf has a weakness that it cannot access multi words. Suppose our string is a complete sentence or a complete paragraph, then it will never be able to be entered. It will never be able to be entered by the user, so there we use a function which is called gets.What does getus do is take a string as input This means we will define a string character ST whose maximum size we will give is 100. Now if we write that string here, then getus will input our string and what put s will do is put s will output our string i.e.