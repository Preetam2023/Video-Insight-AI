ut. File creation exists because the file named New Test does not exist yet, we have not created it. Now here, if we had written w instead of r, then what would have happened for us in write mode. If we want to open this NewTest d txt.gz t EXT, then compile and run, then at the end, what happened is this suddenly.A newly created file has come for us and we can also open this file. Now the file is empty because we have not written anything in it, but that file has been created for us. Next let s talk about reading from a file. Now you have learned to open the file, you have learned to close it, you have learned how to read the data from it. So we have opened it. Now how to read its data, so we read the data with the help of file pointer. First of all, let s say, to read the data, we scan F and print F.