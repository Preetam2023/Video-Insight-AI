ddress, then this chain will continue. Now we have come to the last level of the chain i.e. pointer.Pointer to Pointer We very rarely use pointer to pointers in real life programming but it is important to read them to build up some concept because recruiters can ask if they have seen the top five pointer questions from any website before the interview or your college professor can put it in the syllabus, then how to declare pointer to pointer, use it, let s talk about its syntax.Like we used to write pointers in the form of int star ptr and it has become pointer to pointer, so here instead of one star we will simply put double star, it means pointer to pointer, so if the address of an integer pointer is to be stored then int double star ppt r. If the address of a character pointer is to be stored then character double star ppt. If the floating pointer of a floating variable is to be stored then int double star pptIf you want to store the address, then float double star PPT.