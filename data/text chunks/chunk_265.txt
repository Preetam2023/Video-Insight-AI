y our function definition is also covered. Now the third syntax that we talk about is function call. What does a function call do? We told the compiler that we want to create a function, then we also created the function. Now how to use this function, the work done to use the function is called function call, i.e. calling the function.So how the statement is written for that, we can either call the function from int main or from any other function, so here we are calling another function from main function, we will write the name of our print hello function, we will write parentheses, we will put terminator, then this will become our calling statement which we have written inside int main. Now if you look carefully, our main is also a function whose return type is int because int.value return It does not return int value i.e.